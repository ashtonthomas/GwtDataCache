GetDataCache

This is a sample project that shows one way to implement client side caching 
as a middle layer between GWT Activities and the GWT-RPC Servlets.

This sample allows you to have more hands on control over the caching and clearing
of data in the client application.



